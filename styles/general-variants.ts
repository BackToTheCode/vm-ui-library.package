let variants = {
  indicator: {
    connected: { bg: 'primary' },
    unconnected: { bg: 'warning' },
    disconnected: { bg: 'lightWarning' }
  },
  container: {
    regular: {
      maxWidth: 1024
    },
    wide: {
      maxWidth: '100%'
    },
    regularTall: {
      maxWidth: 1024,
      height: '85vh;'
    }
  }
};

export default variants;
